# Servidor
PORT=3000

# Base de datos (valores por defecto del docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=restaurant_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT — cambia esto por un secreto aleatorio en producción
JWT_SECRET=change_this_to_a_long_random_secret
